#include <iostream>
#include<cmath>


using namespace std;

int main(){
    
    cout<< pow(2,5);
    cout<< sqrt(36);
    cout<< round(4.3); // standard round off to the nearest integer
    cout<< ceil(4.1); // rounds up everything
    cout<< floor(4.8); // rounds down everything both are towards the nearest integer
    cout<< fmax(3,10);
    cout<< fmin(3,10);
    return 0;

    
}